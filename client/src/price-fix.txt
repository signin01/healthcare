// Find and replace all instances where ? is used
// Change this:
<span style={{ fontSize: "20px", fontWeight: "bold", color: "#4caf50" }}>?{product.price}</span>

// To this:
<span style={{ fontSize: "20px", fontWeight: "bold", color: "#4caf50" }}>Rs.{product.price}</span>

// And change:
<p style={{ fontSize: "20px", fontWeight: "bold", color: "#4caf50", margin: "10px 0" }}>?{product.price}</p>

// To this:
<p style={{ fontSize: "20px", fontWeight: "bold", color: "#4caf50", margin: "10px 0" }}>Rs.{product.price}</p>
